variables:
  - name: runId
    value: null
    
steps:
  - task: Bash@3
    inputs:
      targetType: 'inline'
      script: |
        # runId=95
        echo $runId
        echo "##vso[task.setvariable variable=runId;]${runId}"

  - task: Bash@3
    inputs:
      targetType: 'inline'
      script: |
        echo $runId


