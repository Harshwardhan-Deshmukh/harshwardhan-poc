parameters:
  - name: runId
  

steps:
  - task: Bash@3
    inputs:
      targetType: 'inline'
      script: |
        # runId=95
        echo $runId
        echo "##vso[task.setvariable variable=runId;]${{parameters.runId}}"

  - task: Bash@3
    inputs:
      targetType: 'inline'
      script: |
        echo ${{parameters.runId}}


